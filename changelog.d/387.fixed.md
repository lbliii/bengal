The root `bengal --help` now lists `preview` under "Core workflow" and no longer advertises the hidden `health` legacy alias as a separate command, eliminating the stray "Other" section. A guard test keeps the curated help sections in sync with the live command registry.
