Capability ecosystem: registry-driven fence render hooks, theme init wiring, CLI inspection, reference package, site-owner docs, and CDN provisioning policy.

- Third-party packages can ship fence types via `fence_render` on `CapabilitySpec` without editing core (#584)
- Default theme emits vendor assets from `capability_wiring` context instead of hardcoded mermaid/katex/iconify branches (#585)
- New `bengal capability list|info|validate` commands (#588)
- Reference package at `examples/capability-demo/` (#586)
- Site-owner guide at `/docs/building/configuration/capabilities/` (#587)
- `[capabilities.policy]` config for allowed CDN origins or `cdn_mode = "deny"` (#589)
