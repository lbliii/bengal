Built sites using the default theme run less scroll-tracking JavaScript: the reading-progress bar and table-of-contents indicator follow the page with CSS scroll timelines where supported, and the docs sidebar highlights the current section via IntersectionObserver instead of scroll listeners. API hub pages, version notices, cards, dropdowns, and search UI now share one hub-card, admonition, and search styling system.
