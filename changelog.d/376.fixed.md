The shard-worker parse-parity tests (`test_parse_shard_matches_in_process_parse` and the balanced-shard cover) now reset the process-global render caches immediately before the in-process `parse_shard` reparse, not only before the oracle build, closing an order-dependent flake where a prior test left a render-side cache populated that perturbed the section-index child-cards HTML on the reparse but not on the build: a cold build fires a `BUILD_START` cache invalidation that the bare reparse leg did not, so the two parses could diverge under `pytest-randomly` ordering. Both parses now run from the identical clean baseline a real (fresh-process) shard worker guarantees (`#376`).
