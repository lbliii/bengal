Site decomposition + mixin elimination (`#236`): extract `SiteRunner` (build/serve/clean lifecycle) to `bengal.orchestration.site_runner`; `Page`/`Section` now depend on a read-only `SiteContext` Protocol enforced by `import-linter` Contract 2; all 5 Site mixins dissolved into a plain `@dataclass` with zero inheritance (1,649 → ~467 LOC), guarded by `tests/unit/core/test_no_core_mixins.py` and the architectural tenet in `CLAUDE.md`.
