Git versioned builds now prune previously managed stale version outputs, accept `releases/tags` and `git-tags` as aliases for tag-based previous-version discovery, show the version selector on generic versioned pages, and cover repository-root builds where the Bengal site lives in a subdirectory.
