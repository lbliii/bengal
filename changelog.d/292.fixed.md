- Ty diagnostic floor reduced from 531 to 424 (saga #292): `config_service` is now declared on the `SiteConfig`/`SiteLike` protocol (replacing the phantom `paths` member the concrete `Site` never had), `KnowledgeGraph` narrows nullable `GraphAnalyzer`/`GraphReporter` delegates after `@require_built`, and stale per-rule counts in `pyproject.toml` were refreshed against the current breakdown.
