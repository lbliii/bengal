Warm autodoc cache-hit builds no longer crash on a present-but-unreadable source file; both the warm and cold dependency-registration paths now share one OSError-guarded helper and degrade gracefully with an `autodoc_source_stat_failed` warning.
