Removed the obsolete ``@supports (color: color-mix(...))`` feature-query fallbacks throughout the default theme's stylesheets. ``color-mix()`` is now Baseline across supported browsers, so the guarded branches are applied unconditionally and the old-browser fallback blocks are gone.
