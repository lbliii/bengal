`page.authors` and section listing properties (`sorted_pages`, `regular_pages`, `regular_pages_recursive`, `sorted_subsections`, `content_pages`) now return tuples. Copy the collection before mutating it in templates or plugins.
