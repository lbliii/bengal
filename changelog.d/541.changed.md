The default theme search command palette now shows highlighted result snippets and per-heading deep links when heading index records are enabled. Search JavaScript is split into focused modules behind a `<bengal-search>` element, and pressing `?` opens a keyboard-shortcuts help overlay.
