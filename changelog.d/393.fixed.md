Fixed `PatitasParser.render_ast()` so round-tripping `parse_to_ast()` output (including headings, links, lists, and code blocks) renders HTML instead of raising `TypeError: Document.__init__() missing ... 'location'`, honoring the parser's `supports_ast=True` contract.
