# Autodoc Configuration
# ======================
# Generates API documentation from Bengal's Python source and CLI

autodoc:
  # Python API Documentation
  python:
    enabled: true
    source_dirs:
      - ../bengal  # Document the main Bengal package
    output_dir: content/api
    docstring_style: auto  # Auto-detect docstring format

    # Exclude test and cache files
    exclude:
      - "*/tests/*"
      - "*/test_*.py"
      - "*/__pycache__/*"
      - "*/migrations/*"
      - "*/.pytest_cache/*"

    # Member visibility
    include_private: false  # Skip _private methods
    include_special: false  # Skip __special__ methods

    # URL Grouping (NEW!) - Auto-detect groups from package structure
    strip_prefix: "bengal."
    grouping:
      mode: "auto"  # Automatically group by __init__.py hierarchy

  # CLI Documentation
  cli:
    enabled: true
    app_module: "bengal.cli:main"
    framework: click
    output_dir: content/cli
