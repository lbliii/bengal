# Versioning configuration
# See: docs/content/versioning/ for full documentation

versioning:
  enabled: false

  # Sections to version (only these sections get version prefixes)
  sections:
    - docs

  # Version definitions
  versions:
    - id: v0.1.5
      label: "0.1.5"
      latest: true
      # Latest version uses main content/docs/ (no source override needed)

    - id: v0.1.4
      label: "0.1.4"
      deprecated: true
      banner:
        type: warning
        message: "You're viewing docs for Bengal v0.1.4, which is no longer maintained."
      # Older versions live in _versions/<id>/

  # Aliases for friendly URLs
  aliases:
    latest: v0.1.5
    stable: v0.1.5
