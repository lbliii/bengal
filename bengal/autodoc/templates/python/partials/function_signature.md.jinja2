{#-
Function Signature Component

Renders function/method signatures safely with proper code formatting.
Handles cases where signature data is missing or malformed.
-#}

{% from 'macros/safe_macros.md.jinja2' import safe_section %}
{% from 'macros/code_blocks.md.jinja2' import signature_block %}

{% call safe_section("function_signature") %}
{{ signature_block(element.metadata.signature | default(''), element.element_type | default('function')) }}
{% endcall %}
