
---
title: "content"
type: "python-module"
source_file: "bengal/orchestration/content.py"
line_number: 1
description: "Content discovery and setup orchestration for Bengal SSG. Handles content and asset discovery, page/section reference setup, and cascading frontmatter."
---

# content
**Type:** Module
**Source:** [View source](bengal/orchestration/content.py#L1)



**Navigation:**
[bengal](/api/bengal/) ›[orchestration](/api/bengal/orchestration/) ›content

Content discovery and setup orchestration for Bengal SSG.

Handles content and asset discovery, page/section reference setup,
and cascading frontmatter.

## Classes




### `ContentOrchestrator`


Handles content and asset discovery.

Responsibilities:
    - Discover content (pages and sections)
    - Discover assets (site and theme)
    - Set up page/section references for navigation
    - Apply cascading frontmatter from sections to pages









## Methods



#### `__init__`

:::{div} api-badge-group
:::

```python
def __init__(self, site: Site)
```


Initialize content orchestrator.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `site` | `Site` | - | Site instance to populate with content |








#### `discover`

:::{div} api-badge-group
:::

```python
def discover(self, incremental: bool = False, cache: Any | None = None, build_context: BuildContext | None = None) -> None
```


Discover all content and assets.

Main entry point called during build.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `incremental` | `bool` | `False` | Whether this is an incremental build (enables lazy loading) |
| `cache` | `Any \| None` | - | PageDiscoveryCache instance (required if incremental=True) |
| `build_context` | `BuildContext \| None` | - | Optional BuildContext for caching content during discovery. When provided, raw file content is cached for later use by validators, eliminating redundant disk I/O during health checks. |







:::{rubric} Returns
:class: rubric-returns
:::


`None`



#### `discover_content`

:::{div} api-badge-group
:::

```python
def discover_content(self, content_dir: Path | None = None, incremental: bool = False, cache: Any | None = None, build_context: BuildContext | None = None) -> None
```


Discover all content (pages, sections) in the content directory.

Supports optional lazy loading with PageProxy for incremental builds.
When build_context is provided, raw file content is cached for later
use by validators (build-integrated validation).


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `content_dir` | `Path \| None` | - | Content directory path (defaults to root_path/content) |
| `incremental` | `bool` | `False` | Whether this is an incremental build (enables lazy loading) |
| `cache` | `Any \| None` | - | PageDiscoveryCache instance (required if incremental=True) |
| `build_context` | `BuildContext \| None` | - | Optional BuildContext for caching content during discovery. When provided, raw file content is cached for later use by validators, eliminating redundant disk I/O during health checks. |







:::{rubric} Returns
:class: rubric-returns
:::


`None`




#### `discover_assets`

:::{div} api-badge-group
:::

```python
def discover_assets(self, assets_dir: Path | None = None) -> None
```


Discover all assets in the assets directory and theme assets.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `assets_dir` | `Path \| None` | - | Assets directory path (defaults to root_path/assets) |







:::{rubric} Returns
:class: rubric-returns
:::


`None`



---
*Generated by Bengal autodoc from `bengal/orchestration/content.py`*

