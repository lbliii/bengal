
---
title: "static"
type: "python-module"
source_file: "bengal/bengal/orchestration/static.py"
line_number: 1
description: "Static file orchestrator - copies static/ to output verbatim. This enables raw HTML pages, downloadable files, and other assets that need to bypass the content pipeline while still having access to th..."
---

# static
**Type:** Module
**Source:** [View source](bengal/bengal/orchestration/static.py#L1)



**Navigation:**
[bengal](/api/bengal/) ›[orchestration](/api/bengal/orchestration/) ›static

Static file orchestrator - copies static/ to output verbatim.

This enables raw HTML pages, downloadable files, and other assets
that need to bypass the content pipeline while still having access
to theme assets via /assets/css/style.css.

Usage:
    static/demos/holo.html  → public/demos/holo.html
    static/downloads/app.pdf → public/downloads/app.pdf
    static/robots.txt        → public/robots.txt

## Classes




### `StaticOrchestrator`


Copies static files to output directory without processing.









## Methods



#### `__init__`
```python
def __init__(self, site: Site) -> None
```


*No description provided.*


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `site` | `Site` | - | *No description provided.* |







**Returns**


`None`



#### `is_enabled`
```python
def is_enabled(self) -> bool
```


Check if static folder exists and is enabled.



**Returns**


`bool`



#### `get_total_size`
```python
def get_total_size(self) -> int
```


Calculate total size of static folder in bytes.



**Returns**


`int`



#### `copy`
```python
def copy(self) -> int
```


Copy static files to output directory.

Files are copied verbatim without any processing.
Directory structure is preserved.



**Returns**


`int` - Number of files copied



#### `copy_single`
```python
def copy_single(self, rel_path: Path | str) -> bool
```


Copy a single static file (for incremental updates).


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `rel_path` | `Path \| str` | - | Path relative to static folder |







**Returns**


`bool` - True if file was copied successfully



#### `remove_single`
```python
def remove_single(self, rel_path: Path | str) -> bool
```


Remove a single static file from output (when source is deleted).


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `rel_path` | `Path \| str` | - | Path relative to static folder |







**Returns**


`bool` - True if file was removed successfully



---
*Generated by Bengal autodoc from `bengal/bengal/orchestration/static.py`*

