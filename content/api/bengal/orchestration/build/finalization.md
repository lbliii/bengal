
---
title: "finalization"
type: "python-module"
source_file: "bengal/bengal/orchestration/build/finalization.py"
line_number: 1
description: "Finalization phases for build orchestration. Phases 17-21: Post-processing, cache save, collect stats, health check, finalize build."
---

# finalization
**Type:** Module
**Source:** [View source](bengal/bengal/orchestration/build/finalization.py#L1)



**Navigation:**
[bengal](/api/bengal/) ›[orchestration](/api/bengal/orchestration/) ›[build](/api/bengal/orchestration/build/) ›finalization

Finalization phases for build orchestration.

Phases 17-21: Post-processing, cache save, collect stats, health check, finalize build.

## Functions



### `phase_postprocess`


```python
def phase_postprocess(orchestrator: BuildOrchestrator, cli, parallel: bool, ctx, incremental: bool) -> None
```



Phase 17: Post-processing.

Runs post-build tasks: sitemap, RSS, output formats, validation.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `orchestrator` | `BuildOrchestrator` | - | Build orchestrator instance |
| `cli` | - | - | CLI output for user messages |
| `parallel` | `bool` | - | Whether to use parallel processing |
| `ctx` | - | - | Build context |
| `incremental` | `bool` | - | Whether this is an incremental build |







**Returns**


`None`




### `phase_cache_save`


```python
def phase_cache_save(orchestrator: BuildOrchestrator, pages_to_build: list, assets_to_process: list) -> None
```



Phase 18: Save Cache.

Saves build cache for future incremental builds.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `orchestrator` | `BuildOrchestrator` | - | Build orchestrator instance |
| `pages_to_build` | `list` | - | Pages that were built |
| `assets_to_process` | `list` | - | Assets that were processed |







**Returns**


`None`




### `phase_collect_stats`


```python
def phase_collect_stats(orchestrator: BuildOrchestrator, build_start: float) -> None
```



Phase 19: Collect Final Stats.

Collects final build statistics.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `orchestrator` | `BuildOrchestrator` | - | Build orchestrator instance |
| `build_start` | `float` | - | Build start time for duration calculation |







**Returns**


`None`




### `run_health_check`


```python
def run_health_check(orchestrator: BuildOrchestrator, profile: BuildProfile = None, incremental: bool = False) -> None
```



Run health check system with profile-based filtering.

Different profiles run different sets of validators:
- WRITER: Basic checks (broken links, SEO)
- THEME_DEV: Extended checks (performance, templates)
- DEV: Full checks (all validators)


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `orchestrator` | `BuildOrchestrator` | - | Build orchestrator instance |
| `profile` | `BuildProfile` | - | Build profile to use for filtering validators |
| `incremental` | `bool` | `False` | Whether this is an incremental build (enables incremental validation) |







**Returns**


`None`
:::{rubric} Raises
:class: rubric-raises
:::

- **`Exception`**:If strict_mode is enabled and health checks fail





### `phase_finalize`


```python
def phase_finalize(orchestrator: BuildOrchestrator, verbose: bool, collector) -> None
```



Phase 21: Finalize Build.

Performs final cleanup and logging.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `orchestrator` | `BuildOrchestrator` | - | Build orchestrator instance |
| `verbose` | `bool` | - | Whether verbose mode is enabled |
| `collector` | - | - | Performance collector (if enabled) |







**Returns**


`None`



---
*Generated by Bengal autodoc from `bengal/bengal/orchestration/build/finalization.py`*

