
---
title: "registry"
type: "python-module"
source_file: "bengal/bengal/cli/templates/registry.py"
line_number: 1
description: "Template registry and discovery. This module discovers built‑in templates (and optionally custom ones on ``PYTHONPATH``) by importing ``bengal.cli.templates.<name>.template`` and looking for a module‑..."
---

# registry
**Type:** Module
**Source:** [View source](https://github.com/lbliii/bengal/blob/main/bengal/bengal/cli/templates/registry.py#L1)



**Navigation:**
[bengal](/api/bengal/) ›[cli](/api/bengal/cli/) ›[templates](/api/bengal/cli/templates/) ›registry

Template registry and discovery.

This module discovers built‑in templates (and optionally custom ones on
``PYTHONPATH``) by importing ``bengal.cli.templates.<name>.template`` and
looking for a module‑level ``TEMPLATE`` variable. The registry provides a small
API to list, retrieve, and register templates programmatically.

## Classes




### `TemplateRegistry`


Registry for discovering and managing site templates.

The registry scans the sibling directories of this file for template
packages that expose a ``TEMPLATE`` variable.









## Methods



#### `__init__`
```python
def __init__(self)
```


*No description provided.*





#### `get`
```python
def get(self, template_id: str) -> SiteTemplate | None
```


Get a template by its identifier.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `template_id` | `str` | - | The template ID (e.g. ``"blog"``). |







**Returns**


`SiteTemplate | None` - The matching :class:`SiteTemplate` or ``None`` if not found.



#### `list`
```python
def list(self) -> list[tuple[str, str]]
```


List all templates as ``(id, description)`` tuples.



**Returns**


`list[tuple[str, str]]`



#### `exists`
```python
def exists(self, template_id: str) -> bool
```


Return True if a template with ``template_id`` exists.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `template_id` | `str` | - | *No description provided.* |







**Returns**


`bool`

## Functions



### `get_template`


```python
def get_template(template_id: str) -> SiteTemplate | None
```



Convenience wrapper to get a template by ID from the global registry.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `template_id` | `str` | - | *No description provided.* |







**Returns**


`SiteTemplate | None`




### `list_templates`


```python
def list_templates() -> list[tuple[str, str]]
```



Convenience wrapper to list all available templates.



**Returns**


`list[tuple[str, str]]`




### `register_template`


```python
def register_template(template: SiteTemplate) -> None
```



Register a custom template with the global registry.

This allows applications and plugins to add templates at runtime.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `template` | `SiteTemplate` | - | *No description provided.* |







**Returns**


`None`



---
*Generated by Bengal autodoc from `bengal/bengal/cli/templates/registry.py`*

