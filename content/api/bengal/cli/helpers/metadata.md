
---
title: "metadata"
type: "python-module"
source_file: "bengal/cli/helpers/metadata.py"
line_number: 1
description: "Command metadata system for discovery and documentation."
---

# metadata
**Type:** Module
**Source:** [View source](bengal/cli/helpers/metadata.py#L1)



**Navigation:**
[bengal](/api/bengal/) ›[cli](/api/bengal/cli/) ›[helpers](/api/bengal/cli/helpers/) ›metadata

Command metadata system for discovery and documentation.

## Classes




### `CommandMetadata`


Metadata for a CLI command.


:::{info}
This is a dataclass.
:::



**Attributes:**

:::{div} api-attributes
`category`
: 

`description`
: 

`examples`
: 

`requires_site`
: 

`requires_build`
: 

`tags`
: 

`aliases`
: 

:::







## Methods



#### `to_dict`

:::{div} api-badge-group
:::

```python
def to_dict(self) -> dict[str, Any]
```


Convert metadata to dictionary.



:::{rubric} Returns
:class: rubric-returns
:::


`dict[str, Any]`

## Functions



### `command_metadata`


```python
def command_metadata(category: str = 'general', description: str = '', examples: list[str] | None = None, requires_site: bool = False, requires_build: bool = False, tags: list[str] | None = None, aliases: list[str] | None = None) -> Callable[[F], F]
```



Decorator to attach metadata to a CLI command.


**Parameters**

::::{dropdown} 7 parameters (click to expand)
:open: true

**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `category` | `str` | `'general'` | Command category (e.g., 'build', 'content', 'config', 'dev') |
| `description` | `str` | `''` | Short description of what the command does |
| `examples` | `list[str] \| None` | - | Example usage strings |
| `requires_site` | `bool` | `False` | Whether this command requires a site to be loaded |
| `requires_build` | `bool` | `False` | Whether this command requires the site to be built first |
| `tags` | `list[str] \| None` | - | Tags for filtering/discovery (e.g., ['dev', 'content', 'quick']) |
| `aliases` | `list[str] \| None` | - | Command aliases (for discovery) |



::::






**Returns**


`Callable[[F], F]`
:::{rubric} Examples
:class: rubric-examples
:::


```python
@click.command()
    @command_metadata(
        category="build",
        description="Build the static site",
        examples=["bengal build", "bengal build --incremental"],
        requires_site=True,
        tags=["build", "production"]
    )
    def build():
        # ...
        pass
```





### `get_command_metadata`


```python
def get_command_metadata(cmd: click.Command | Callable[..., Any]) -> CommandMetadata | None
```



Get metadata from a command.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `cmd` | `click.Command \| Callable[..., Any]` | - | Click command or callable |







**Returns**


`CommandMetadata | None` - CommandMetadata if found, None otherwise




### `list_commands_by_category`


```python
def list_commands_by_category(group: click.Group) -> dict[str, list[click.Command]]
```



List all commands in a group, organized by category.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `group` | `click.Group` | - | Click group to scan |







**Returns**


`dict[str, list[click.Command]]` - Dictionary mapping category names to lists of commands




### `find_commands_by_tag`


```python
def find_commands_by_tag(group: click.Group, tag: str) -> list[tuple[str, click.Command]]
```



Find commands that have a specific tag.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `group` | `click.Group` | - | Click group to scan |
| `tag` | `str` | - | Tag to search for |







**Returns**


`list[tuple[str, click.Command]]` - List of (command_path, command) tuples



---
*Generated by Bengal autodoc from `bengal/cli/helpers/metadata.py`*

