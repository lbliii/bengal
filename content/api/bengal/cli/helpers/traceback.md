
---
title: "traceback"
type: "python-module"
source_file: "bengal/bengal/cli/helpers/traceback.py"
line_number: 1
description: "Helper for configuring traceback behavior in CLI commands."
---

# traceback
**Type:** Module
**Source:** [View source](bengal/bengal/cli/helpers/traceback.py#L1)



**Navigation:**
[bengal](/api/bengal/) ›[cli](/api/bengal/cli/) ›[helpers](/api/bengal/cli/helpers/) ›traceback

Helper for configuring traceback behavior in CLI commands.

## Functions



### `configure_traceback`


```python
def configure_traceback(debug: bool = False, traceback: str | None = None, site: Site | None = None) -> None
```



Configure traceback handling with proper precedence.

Precedence order:
1. CLI --traceback flag (highest)
2. CLI --debug flag (maps to full traceback)
3. File-based config ([dev.traceback] in site config)
4. Environment variable (BENGAL_TRACEBACK)
5. Default (minimal)


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `debug` | `bool` | `False` | Whether debug mode is enabled (maps to full traceback) |
| `traceback` | `str \| None` | - | Explicit traceback style (full, compact, minimal, off) |
| `site` | `Site \| None` | - | Optional Site instance to apply file-based config from |







**Returns**


`None`
:::{rubric} Examples
:class: rubric-examples
:::


```python
@click.command()
    @click.option("--debug", is_flag=True)
    @click.option("--traceback", type=click.Choice([...]))
    def my_command(debug: bool, traceback: str | None):
        configure_traceback(debug, traceback)
        # ... rest of command ...
```



---
*Generated by Bengal autodoc from `bengal/bengal/cli/helpers/traceback.py`*
