
---
title: "sections"
type: "python-module"
source_file: "../bengal/bengal/utils/sections.py"
line_number: 1
description: "Section-related utility helpers. Provides safe normalization helpers for section references that may be either `Section` objects or precomputed string paths depending on context (e.g., runtime vs cach..."
---

# sections
**Type:** Module
**Source:** [View source](../bengal/bengal/utils/sections.py#L1)



**Navigation:**
[bengal](/api/bengal/) ›[utils](/api/bengal/utils/) ›sections

Section-related utility helpers.

Provides safe normalization helpers for section references that may be either
`Section` objects or precomputed string paths depending on context (e.g.,
runtime vs cached/incremental data structures).

## Functions



### `resolve_page_section_path`


```python
def resolve_page_section_path(page: Any) -> str | None
```



Resolve a page's section path as a string, handling multiple representations.

The page may expose its section association in different ways depending on
build phase or caching:
- `page.section` may be a `Section` object with a `.path` attribute
- `page.section` may already be a string path
- It may be missing or falsy for root-level pages


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `page` | `Any` | - | Page-like object which may have a `section` attribute |







**Returns**


`str | None` - String path to the section (e.g., "docs/tutorials") or None if not set.



---
*Generated by Bengal autodoc from `../bengal/bengal/utils/sections.py`*
