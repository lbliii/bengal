
---
title: "rest"
type: "python-module"
source_file: "bengal/bengal/content_layer/sources/rest.py"
line_number: 1
description: "RESTSource - Content source for REST APIs. Fetches content from any REST API that returns JSON, with configurable field mappings for content and frontmatter. Requires: pip install bengal[rest] (instal..."
---

# rest
**Type:** Module
**Source:** [View source](bengal/bengal/content_layer/sources/rest.py#L1)



**Navigation:**
[bengal](/api/bengal/) ›[content_layer](/api/bengal/content_layer/) ›[sources](/api/bengal/content_layer/sources/) ›rest

RESTSource - Content source for REST APIs.

Fetches content from any REST API that returns JSON, with
configurable field mappings for content and frontmatter.

Requires: pip install bengal[rest] (installs aiohttp)

## Classes




### `RESTSource`


**Inherits from:**`ContentSource`Content source for REST APIs.

Fetches content from any REST API that returns JSON. Supports:
- Custom headers (with environment variable expansion)
- Configurable field mappings for content and frontmatter
- Pagination (link header or cursor-based)

Configuration:
    url: str - API endpoint URL (required)
    headers: dict - Request headers (optional, supports ${ENV_VAR})
    content_field: str - JSON path to content (default: "content")
    id_field: str - JSON path to ID (default: "id")
    frontmatter_fields: dict - Mapping of frontmatter keys to JSON paths
    items_path: str - JSON path to items array (default: auto-detect)
    pagination: dict - Pagination config (optional)
        strategy: str - "link_header" or "cursor"
        cursor_field: str - Field containing next cursor









## Methods



#### `__init__`
```python
def __init__(self, name: str, config: dict[str, Any]) -> None
```


Initialize REST source.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `name` | `str` | - | Source name |
| `config` | `dict[str, Any]` | - | Configuration with 'url' required |







**Returns**


`None`



#### `fetch_all` @async
```python
async def fetch_all(self) -> AsyncIterator[ContentEntry]
```


Fetch all content from the API.

Handles pagination automatically if configured.

:::{info} Async Method
This is an asynchronous method. Use `await` when calling.
:::



**Returns**


`AsyncIterator[ContentEntry]`



#### `fetch_one` @async
```python
async def fetch_one(self, id: str) -> ContentEntry | None
```


Fetch a single item by ID.

:::{info} Async Method
This is an asynchronous method. Use `await` when calling.
:::


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `id` | `str` | - | Item identifier |







**Returns**


`ContentEntry | None` - ContentEntry if found, None otherwise



---
*Generated by Bengal autodoc from `bengal/bengal/content_layer/sources/rest.py`*

