
---
title: "notion"
type: "python-module"
source_file: "bengal/content_layer/sources/notion.py"
line_number: 1
description: "NotionSource - Content source for Notion databases. Fetches pages from Notion databases and converts them to markdown. Requires: pip install bengal[notion] (installs aiohttp)"
---

# notion
**Type:** Module
**Source:** [View source](bengal/content_layer/sources/notion.py#L1)



**Navigation:**
[bengal](/api/bengal/) ›[content_layer](/api/bengal/content_layer/) ›[sources](/api/bengal/content_layer/sources/) ›notion

NotionSource - Content source for Notion databases.

Fetches pages from Notion databases and converts them to markdown.

Requires: pip install bengal[notion] (installs aiohttp)

## Classes




### `NotionSource`


**Inherits from:**`ContentSource`Content source for Notion databases.

Fetches pages from a Notion database and converts them to markdown.
Requires a Notion integration token with read access to the database.

Configuration:
    database_id: str - Notion database ID (required)
    token: str - Notion integration token (or NOTION_TOKEN env var)
    property_mapping: dict - Map Notion properties to frontmatter fields
        Defaults: {"title": "Name", "date": "Date", "tags": "Tags"}
    filter: dict - Notion filter object (optional)
    sorts: list - Notion sorts array (optional)

Setup:
    1. Create a Notion integration at https://www.notion.so/my-integrations
    2. Share your database with the integration
    3. Set NOTION_TOKEN environment variable or pass token in config









## Methods



#### `__init__`

:::{div} api-badge-group
:::

```python
def __init__(self, name: str, config: dict[str, Any]) -> None
```


Initialize Notion source.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `name` | `str` | - | Source name |
| `config` | `dict[str, Any]` | - | Configuration with 'database_id' required |







:::{rubric} Returns
:class: rubric-returns
:::


`None`



#### `fetch_all`

:::{div} api-badge-group
<span class="api-badge api-badge-async">async</span>:::

```python
async def fetch_all(self) -> AsyncIterator[ContentEntry]
```


Fetch all pages from the database.

Handles pagination automatically.

:::{info} Async Method
This is an asynchronous method. Use `await` when calling.
:::



:::{rubric} Returns
:class: rubric-returns
:::


`AsyncIterator[ContentEntry]`



#### `fetch_one`

:::{div} api-badge-group
<span class="api-badge api-badge-async">async</span>:::

```python
async def fetch_one(self, id: str) -> ContentEntry | None
```


Fetch a single page by ID.

:::{info} Async Method
This is an asynchronous method. Use `await` when calling.
:::


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `id` | `str` | - | Notion page ID |







:::{rubric} Returns
:class: rubric-returns
:::


`ContentEntry | None` - ContentEntry if found, None otherwise



---
*Generated by Bengal autodoc from `bengal/content_layer/sources/notion.py`*

