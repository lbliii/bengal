
---
title: "github"
type: "python-module"
source_file: "bengal/bengal/content_layer/sources/github.py"
line_number: 1
description: "GitHubSource - Content source for GitHub repositories. Fetches markdown files from GitHub repos, supporting both public and private repositories with token authentication. Requires: pip install bengal..."
---

# github
**Type:** Module
**Source:** [View source](bengal/bengal/content_layer/sources/github.py#L1)



**Navigation:**
[bengal](/api/bengal/) ›[content_layer](/api/bengal/content_layer/) ›[sources](/api/bengal/content_layer/sources/) ›github

GitHubSource - Content source for GitHub repositories.

Fetches markdown files from GitHub repos, supporting both public
and private repositories with token authentication.

Requires: pip install bengal[github] (installs aiohttp)

## Classes




### `GitHubSource`


**Inherits from:**`ContentSource`Content source for GitHub repositories.

Fetches markdown files from a GitHub repo using the GitHub API.
Supports both public repos and private repos with token authentication.

Configuration:
    repo: str - Repository in "owner/repo" format (required)
    branch: str - Branch name (default: "main")
    path: str - Directory path within repo (default: "")
    token: str - GitHub token (optional, uses GITHUB_TOKEN env var)
    glob: str - File pattern to match (default: "*.md")









## Methods



#### `__init__`
```python
def __init__(self, name: str, config: dict[str, Any]) -> None
```


Initialize GitHub source.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `name` | `str` | - | Source name |
| `config` | `dict[str, Any]` | - | Configuration with 'repo' required |







**Returns**


`None`
:::{rubric} Raises
:class: rubric-raises
:::

- **`ValueError`**:If 'repo' not specified




#### `fetch_all` @async
```python
async def fetch_all(self) -> AsyncIterator[ContentEntry]
```


Fetch all markdown files from the repository.

:::{info} Async Method
This is an asynchronous method. Use `await` when calling.
:::



**Returns**


`AsyncIterator[ContentEntry]`



#### `fetch_one` @async
```python
async def fetch_one(self, id: str) -> ContentEntry | None
```


Fetch a single file by path.

:::{info} Async Method
This is an asynchronous method. Use `await` when calling.
:::


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `id` | `str` | - | Relative path within the configured path |







**Returns**


`ContentEntry | None` - ContentEntry if found, None otherwise




#### `get_last_modified` @async
```python
async def get_last_modified(self) -> datetime | None
```


Get latest commit time for the configured path.

:::{info} Async Method
This is an asynchronous method. Use `await` when calling.
:::



**Returns**


`datetime | None` - Datetime of most recent commit or None



#### `is_changed` @async
```python
async def is_changed(self, cached_checksum: str | None) -> bool
```


Check if repo has changed since last fetch.

Uses latest commit SHA for the path.

:::{info} Async Method
This is an asynchronous method. Use `await` when calling.
:::


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `cached_checksum` | `str \| None` | - | Previous commit SHA |







**Returns**


`bool` - True if changed or unknown



---
*Generated by Bengal autodoc from `bengal/bengal/content_layer/sources/github.py`*

