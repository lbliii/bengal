
---
title: "local"
type: "python-module"
source_file: "bengal/bengal/content_layer/sources/local.py"
line_number: 1
description: "LocalSource - Content source for local filesystem. This is the default source, reading markdown files from a directory. No external dependencies required."
---

# local
**Type:** Module
**Source:** [View source](bengal/bengal/content_layer/sources/local.py#L1)



**Navigation:**
[bengal](/api/bengal/) ›[content_layer](/api/bengal/content_layer/) ›[sources](/api/bengal/content_layer/sources/) ›local

LocalSource - Content source for local filesystem.

This is the default source, reading markdown files from a directory.
No external dependencies required.

## Classes




### `LocalSource`


**Inherits from:**`ContentSource`Content source for local filesystem.

Reads markdown files from a directory, parsing frontmatter and
generating content entries.

Configuration:
    directory: str - Directory path (relative to site root)
    glob: str - Glob pattern for matching files (default: "**/*.md")
    exclude: list[str] - Patterns to exclude (default: [])









## Methods



#### `__init__`
```python
def __init__(self, name: str, config: dict[str, Any]) -> None
```


Initialize local source.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `name` | `str` | - | Source name |
| `config` | `dict[str, Any]` | - | Configuration with 'directory' key required |







**Returns**


`None`
:::{rubric} Raises
:class: rubric-raises
:::

- **`ValueError`**:If 'directory' not specified




#### `fetch_all` @async
```python
async def fetch_all(self) -> AsyncIterator[ContentEntry]
```


Fetch all content entries from this directory.

:::{info} Async Method
This is an asynchronous method. Use `await` when calling.
:::



**Returns**


`AsyncIterator[ContentEntry]`



#### `fetch_one` @async
```python
async def fetch_one(self, id: str) -> ContentEntry | None
```


Fetch a single file by relative path.

:::{info} Async Method
This is an asynchronous method. Use `await` when calling.
:::


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `id` | `str` | - | Relative path from directory (e.g., "getting-started.md") |







**Returns**


`ContentEntry | None` - ContentEntry if found, None otherwise






#### `get_last_modified` @async
```python
async def get_last_modified(self) -> datetime | None
```


Get most recent modification time of any file.

:::{info} Async Method
This is an asynchronous method. Use `await` when calling.
:::



**Returns**


`datetime | None` - Most recent mtime or None



---
*Generated by Bengal autodoc from `bengal/bengal/content_layer/sources/local.py`*
