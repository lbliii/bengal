
---
title: "link_validator"
type: "python-module"
source_file: "bengal/bengal/rendering/link_validator.py"
line_number: 1
description: "Link validation for catching broken links. This module validates internal links in pages against the site\'s page URLs to catch broken links during build time. Key features: - Validates internal links ..."
---

# link_validator
**Type:** Module
**Source:** [View source](https://github.com/lbliii/bengal/blob/main/bengal/bengal/rendering/link_validator.py#L1)



**Navigation:**
[bengal](/api/bengal/) ›[rendering](/api/bengal/rendering/) ›link_validator

Link validation for catching broken links.

This module validates internal links in pages against the site's page URLs
to catch broken links during build time.

Key features:
- Validates internal links resolve to existing pages
- Handles relative paths and fragments
- Supports trailing slash variations
- Caches validation results for performance

## Classes




### `LinkValidator`


Validates links in pages to catch broken links.

This validator checks that internal links resolve to existing pages
in the site. External links (http/https), mailto, tel, and fragment-only
links are skipped (external link checking is handled by health/validators/links.py).



**Attributes:**

| Name | Type | Description |
|:-----|:-----|:------------|
| `validated_urls` | - | Set of URLs that have been validated (cache) |
| `broken_links` | - | List of (page_path, broken_link) tuples |
| `_page_urls` | - | Cached set of all page URLs for O(1) lookup |
| `_site` | - | Reference to site being validated |







## Methods



#### `__init__`
```python
def __init__(self, site: Site | None = None) -> None
```


Initialize the link validator.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `site` | `Site \| None` | - | Optional Site instance for URL resolution |







**Returns**


`None`




#### `validate_page_links`
```python
def validate_page_links(self, page: Page, site: Any | None = None) -> list[str]
```


Validate all links in a page.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `page` | `Page` | - | Page to validate |
| `site` | `Any \| None` | - | Optional Site instance (uses cached if not provided) |







**Returns**


`list[str]` - List of broken link URLs



#### `validate_site`
```python
def validate_site(self, site: Any) -> list[tuple]
```


Validate all links in the entire site.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `site` | `Any` | - | Site instance |







**Returns**


`list[tuple]` - List of (page_path, broken_link) tuples



---
*Generated by Bengal autodoc from `bengal/bengal/rendering/link_validator.py`*

