
---
title: "tables"
type: "python-module"
source_file: "bengal/rendering/template_functions/tables.py"
line_number: 1
description: "Table functions for templates. Provides functions for rendering interactive data tables from YAML/CSV files."
---

# tables
**Type:** Module
**Source:** [View source](bengal/rendering/template_functions/tables.py#L1)



**Navigation:**
[bengal](/api/bengal/) ›[rendering](/api/bengal/rendering/) ›[template_functions](/api/bengal/rendering/template_functions/) ›tables

Table functions for templates.

Provides functions for rendering interactive data tables from YAML/CSV files.

## Functions



### `register`


```python
def register(env: Environment, site: Site) -> None
```



Register table functions with Jinja2 environment.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `env` | `Environment` | - | Jinja2 environment |
| `site` | `Site` | - | Site instance |







**Returns**


`None`




### `data_table`


```python
def data_table(env: Environment, path: str, **options: Any) -> Markup
```



Render interactive data table from YAML or CSV file.

Uses the same underlying implementation as the data-table directive,
but can be called directly from templates for more flexibility.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `env` | `Environment` | - | Jinja2 environment (injected) |
| `path` | `str` | - | Relative path to data file (YAML or CSV) **options: Table options - search (bool): Enable search box (default: True) - filter (bool): Enable column filters (default: True) - sort (bool): Enable column sorting (default: True) - pagination (int\|False): Rows per page, or False to disable (default: 50) - height (str): Table height like "400px" (default: "auto") - columns (str): Comma-separated list of columns to show (default: all) |







**Returns**


`Markup` - Markup object with rendered HTML table
:::{rubric} Examples
:class: rubric-examples
:::


```python
{# Basic usage #}
    {{ data_table('data/browser-support.yaml') }}

    {# With options #}
    {{ data_table('data/hardware-specs.csv',
                  pagination=100,
                  height='500px',
                  search=True) }}

    {# Show specific columns only #}
    {{ data_table('data/support-matrix.yaml',
                  columns='Feature,Chrome,Firefox') }}
```



---
*Generated by Bengal autodoc from `bengal/rendering/template_functions/tables.py`*

