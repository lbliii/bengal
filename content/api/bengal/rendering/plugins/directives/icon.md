
---
title: "icon"
type: "python-module"
source_file: "bengal/bengal/rendering/plugins/directives/icon.py"
line_number: 1
description: "Icon directive for Mistune. Provides inline SVG icons from Bengal\'s icon library: ```{icon} name``` Syntax: ```{icon} terminal :size: 24 :class: my-icon-class ``` Supports looking up icons from the th..."
---

# icon
**Type:** Module
**Source:** [View source](bengal/bengal/rendering/plugins/directives/icon.py#L1)



**Navigation:**
[bengal](/api/bengal/) ›[rendering](/api/bengal/rendering/) ›[plugins](/api/bengal/rendering/plugins/) ›[directives](/api/bengal/rendering/plugins/directives/) ›icon

Icon directive for Mistune.

Provides inline SVG icons from Bengal's icon library: ```{icon} name```

Syntax:
    ```{icon} terminal
    :size: 24
    :class: my-icon-class
    ```

Supports looking up icons from the theme's `assets/icons/` directory
and inlining them as SVG for full CSS styling support with `currentColor`.

## Classes




### `IconDirective`


**Inherits from:**`DirectivePlugin`Icon directive for inline SVG icons.

Syntax:
    ```{icon} terminal
    ```

    ```{icon} docs
    :size: 16
    :class: text-muted
    ```

Options:
    :size: Icon size in pixels (default: 24)
    :class: Additional CSS classes
    :aria-label: Accessibility label (default: icon name)

The icon is inlined as an SVG element with:
    - width/height set to specified size
    - class="bengal-icon icon-{name} {extra-classes}"
    - aria-hidden="true" (unless aria-label provided)
    - fill="currentColor" on applicable elements

Available icons are loaded from the theme's `assets/icons/` directory.









## Methods



#### `parse`
```python
def parse(self, block: Any, m: Any, state: Any) -> dict[str, Any]
```


Parse icon directive.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `block` | `Any` | - | Block parser |
| `m` | `Any` | - | Regex match object |
| `state` | `Any` | - | Parser state |







**Returns**


`dict[str, Any]` - Dict with icon data for rendering



#### `__call__`
```python
def __call__(self, directive: Any, md: Any) -> None
```


Register icon directive with Mistune.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `directive` | `Any` | - | FencedDirective instance |
| `md` | `Any` | - | Markdown instance |







**Returns**


`None`

## Functions



### `get_available_icons`


```python
def get_available_icons() -> list[str]
```



Get list of available icon names.



**Returns**


`list[str]` - List of icon names (without .svg extension)




### `render_icon`


```python
def render_icon(renderer: Any, text: str, **attrs: Any) -> str
```



Render icon directive to inline SVG.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `renderer` | `Any` | - | Mistune renderer |
| `text` | `str` | - | Rendered children content (unused for icons) **attrs: Directive attributes (name, size, class, aria_label) |







**Returns**


`str` - Inline SVG HTML or empty span with error class



---
*Generated by Bengal autodoc from `bengal/bengal/rendering/plugins/directives/icon.py`*
