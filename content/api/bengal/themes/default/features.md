
---
title: "features"
type: "python-module"
source_file: "bengal/bengal/themes/default/features.py"
line_number: 1
description: "Default theme feature registry. Each feature has: - key: Dotted namespace string (e.g., \"navigation.breadcrumbs\") - description: Human-readable description - default: Whether enabled by default (optio..."
---

# features
**Type:** Module
**Source:** [View source](bengal/bengal/themes/default/features.py#L1)



**Navigation:**
[bengal](/api/bengal/) ›[themes](/api/bengal/themes/) ›[default](/api/bengal/themes/default/) ›features

Default theme feature registry.

Each feature has:
- key: Dotted namespace string (e.g., "navigation.breadcrumbs")
- description: Human-readable description
- default: Whether enabled by default (optional, defaults to False)
- category: Category for grouping in CLI output

Feature flags allow users to declaratively enable/disable theme behaviors
via config rather than editing templates.

Example:
    [theme]
    features = [
        "navigation.breadcrumbs",
        "navigation.toc",
        "content.code.copy",
    ]

Templates check features via:
    {% if 'navigation.toc' in site.theme_config.features %}

## Classes




### `FeatureInfo`


Information about a theme feature.



**Attributes:**

| Name | Type | Description |
|:-----|:-----|:------------|
| `key` | - | Dotted namespace string (e.g., "navigation.breadcrumbs") |
| `description` | - | Human-readable description of what the feature does |
| `default` | - | Whether this feature is enabled by default |
| `category` | - | Category for grouping (navigation, content, search, etc.) |

## Functions



### `get_default_features`


```python
def get_default_features() -> list[str]
```



Get list of feature keys that are enabled by default.



**Returns**


`list[str]` - List of feature keys (e.g., ["navigation.toc", "content.code.copy"])
:::{rubric} Examples
:class: rubric-examples
:::


```python
>>> defaults = get_default_features()
    >>> "navigation.toc" in defaults
    True
    >>> "navigation.tabs" in defaults
    False
```





### `validate_features`


```python
def validate_features(features: list[str]) -> list[str]
```



Validate feature list and return unknown features.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `features` | `list[str]` | - | List of feature keys to validate |







**Returns**


`list[str]` - List of unknown feature keys (empty if all valid)
:::{rubric} Examples
:class: rubric-examples
:::


```python
>>> validate_features(["navigation.toc", "invalid.feature"])
    ['invalid.feature']
    >>> validate_features(["navigation.toc", "content.code.copy"])
    []
```





### `get_features_by_category`


```python
def get_features_by_category() -> dict[str, list[FeatureInfo]]
```



Get features grouped by category.



**Returns**


`dict[str, list[FeatureInfo]]` - Dictionary mapping category names to list of FeatureInfo objects
:::{rubric} Examples
:class: rubric-examples
:::


```python
>>> by_cat = get_features_by_category()
    >>> "navigation" in by_cat
    True
    >>> len(by_cat["navigation"]) > 0
    True
```





### `get_feature_info`


```python
def get_feature_info(feature: str) -> FeatureInfo | None
```



Get information about a specific feature.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `feature` | `str` | - | Feature key (e.g., "navigation.toc") |







**Returns**


`FeatureInfo | None` - FeatureInfo object if found, None otherwise
:::{rubric} Examples
:class: rubric-examples
:::


```python
>>> info = get_feature_info("navigation.toc")
    >>> info.description
    'Show table of contents sidebar'
```





### `get_all_feature_keys`


```python
def get_all_feature_keys() -> list[str]
```



Get all available feature keys.



**Returns**


`list[str]` - List of all feature keys in alphabetical order
:::{rubric} Examples
:class: rubric-examples
:::


```python
>>> keys = get_all_feature_keys()
    >>> "navigation.toc" in keys
    True
```



---
*Generated by Bengal autodoc from `bengal/bengal/themes/default/features.py`*
