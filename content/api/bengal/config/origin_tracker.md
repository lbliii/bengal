
---
title: "origin_tracker"
type: "python-module"
source_file: "../bengal/bengal/config/origin_tracker.py"
line_number: 1
description: "Origin tracking for config introspection. Tracks which file contributed each configuration key for debugging and the `bengal config show --origin` command."
---

# origin_tracker
**Type:** Module
**Source:** [View source](../bengal/bengal/config/origin_tracker.py#L1)



**Navigation:**
[bengal](/api/bengal/) ›[config](/api/bengal/config/) ›origin_tracker

Origin tracking for config introspection.

Tracks which file contributed each configuration key for debugging
and the `bengal config show --origin` command.

## Classes




### `ConfigWithOrigin`


Configuration with origin tracking.

Tracks which file (or source) contributed each configuration key
for introspection and debugging.









## Methods



#### `__init__`
```python
def __init__(self) -> None
```


Initialize empty config with origin tracking.



**Returns**


`None`



#### `merge`
```python
def merge(self, other: dict[str, Any], origin: str) -> None
```


Merge config and track origin.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `other` | `dict[str, Any]` | - | Config dict to merge in |
| `origin` | `str` | - | Source identifier (e.g., "_default/site.yaml") |







**Returns**


`None`




#### `show_with_origin`
```python
def show_with_origin(self, indent: int = 0) -> str
```


Format config with origin annotations.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `indent` | `int` | `0` | Starting indentation level |







**Returns**


`str` - Formatted string with origins as comments
:::{rubric} Examples
:class: rubric-examples
:::


```python
>>> tracker.show_with_origin()
    site:
      title: Test  # _default/site.yaml
      baseurl: https://example.com  # environments/production.yaml
```





#### `get_origin`
```python
def get_origin(self, key_path: str) -> str | None
```


Get origin for a specific key path.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `key_path` | `str` | - | Dot-separated key path (e.g., "site.title") |







**Returns**


`str | None` - Origin string, or None if not found



---
*Generated by Bengal autodoc from `../bengal/bengal/config/origin_tracker.py`*
