
---
title: "environment"
type: "python-module"
source_file: "bengal/config/environment.py"
line_number: 1
description: "Environment detection for config system. Automatically detects deployment environment from platform-specific environment variables (Netlify, Vercel, GitHub Actions, etc)."
---

# environment
**Type:** Module
**Source:** [View source](bengal/config/environment.py#L1)



**Navigation:**
[bengal](/api/bengal/) ›[config](/api/bengal/config/) ›environment

Environment detection for config system.

Automatically detects deployment environment from platform-specific
environment variables (Netlify, Vercel, GitHub Actions, etc).

## Functions



### `detect_environment`


```python
def detect_environment() -> str
```



Detect deployment environment from platform signals.

Checks in order:
1. BENGAL_ENV (explicit override)
2. Netlify (NETLIFY=true + CONTEXT)
3. Vercel (VERCEL=1 + VERCEL_ENV)
4. GitHub Actions (GITHUB_ACTIONS=true)
5. Default: "local"



**Returns**


`str` - Environment name: "local", "preview", or "production"
:::{rubric} Examples
:class: rubric-examples
:::


```python
>>> os.environ["BENGAL_ENV"] = "production"
    >>> detect_environment()
    "production"

    >>> os.environ.clear()
    >>> os.environ["NETLIFY"] = "true"
    >>> os.environ["CONTEXT"] = "production"
    >>> detect_environment()
    "production"
```





### `get_environment_file_candidates`


```python
def get_environment_file_candidates(environment: str) -> list[str]
```



Get candidate filenames for environment config.

Returns list in priority order (first match wins).


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `environment` | `str` | - | Environment name (e.g., "production") |







**Returns**


`list[str]` - List of candidate filenames
:::{rubric} Examples
:class: rubric-examples
:::


```python
>>> get_environment_file_candidates("production")
    ["production.yaml", "production.yml", "prod.yaml", "prod.yml"]
```



---
*Generated by Bengal autodoc from `bengal/config/environment.py`*

