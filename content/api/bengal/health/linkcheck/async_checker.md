
---
title: "async_checker"
type: "python-module"
source_file: "bengal/bengal/health/linkcheck/async_checker.py"
line_number: 1
description: "Async external link checker with retries, backoff, and concurrency control."
---

# async_checker
**Type:** Module
**Source:** [View source](bengal/bengal/health/linkcheck/async_checker.py#L1)



**Navigation:**
[bengal](/api/bengal/) ›[health](/api/bengal/health/) ›[linkcheck](/api/bengal/health/linkcheck/) ›async_checker

Async external link checker with retries, backoff, and concurrency control.

## Classes




### `AsyncLinkChecker`


Async HTTP link checker with retries, backoff, and concurrency control.

Features:
- Global concurrency limit via semaphore
- Per-host concurrency limit to avoid rate limits
- Exponential backoff with jitter for retries
- HEAD request first, fallback to GET on 405/403
- Connection pooling and DNS caching via httpx
- Ignore policies for patterns, domains, and status ranges









## Methods



#### `__init__`
```python
def __init__(self, max_concurrency: int = 20, per_host_limit: int = 4, timeout: float = 10.0, retries: int = 2, retry_backoff: float = 0.5, ignore_policy: IgnorePolicy | None = None, user_agent: str = 'Bengal-LinkChecker/1.0')
```


Initialize async link checker.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `max_concurrency` | `int` | `20` | Maximum concurrent requests across all hosts |
| `per_host_limit` | `int` | `4` | Maximum concurrent requests per host |
| `timeout` | `float` | `10.0` | Request timeout in seconds |
| `retries` | `int` | `2` | Number of retry attempts |
| `retry_backoff` | `float` | `0.5` | Base backoff time for exponential backoff (seconds) |
| `ignore_policy` | `IgnorePolicy \| None` | - | Policy for ignoring links/statuses |
| `user_agent` | `str` | `'Bengal-LinkChecker/1.0'` | User-Agent header value |








#### `check_links` @async
```python
async def check_links(self, urls: list[tuple[str, str]]) -> dict[str, LinkCheckResult]
```


Check multiple external URLs concurrently.

:::{info} Async Method
This is an asynchronous method. Use `await` when calling.
:::


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `urls` | `list[tuple[str, str]]` | - | List of (url, first_ref) tuples where first_ref is the page that first referenced this URL |







**Returns**


`dict[str, LinkCheckResult]` - Dict mapping URL to LinkCheckResult






#### `from_config` @classmethod
```python
def from_config(cls, config: dict[str, Any]) -> AsyncLinkChecker
```


Create AsyncLinkChecker from config dict.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `config` | `dict[str, Any]` | - | Configuration dict |







**Returns**


`AsyncLinkChecker` - Configured AsyncLinkChecker instance



---
*Generated by Bengal autodoc from `bengal/bengal/health/linkcheck/async_checker.py`*

