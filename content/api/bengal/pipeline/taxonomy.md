
---
title: "taxonomy"
type: "python-module"
source_file: "bengal/bengal/pipeline/taxonomy.py"
line_number: 1
description: "Taxonomy stream for collecting tags and generating taxonomy pages. This module provides stream-based taxonomy processing that replaces TaxonomyOrchestrator with a declarative, reactive approach. Flow:..."
---

# taxonomy
**Type:** Module
**Source:** [View source](bengal/bengal/pipeline/taxonomy.py#L1)



**Navigation:**
[bengal](/api/bengal/) ›[pipeline](/api/bengal/pipeline/) ›taxonomy

Taxonomy stream for collecting tags and generating taxonomy pages.

This module provides stream-based taxonomy processing that replaces
TaxonomyOrchestrator with a declarative, reactive approach.

Flow:
    pages → collect_tags → group_by_tag → generate_taxonomy_pages → taxonomy_pages

## Functions



### `create_taxonomy_stream`


```python
def create_taxonomy_stream(pages_stream: Stream[Page], site: Site) -> Stream[Page]
```



Create a stream that collects taxonomies and generates taxonomy pages.

This stream:
1. Collects all tags from pages
2. Groups pages by tag
3. Generates tag index page
4. Generates individual tag pages (with pagination)


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `pages_stream` | `Stream[Page]` | - | Stream of Page objects |
| `site` | `Site` | - | Site instance (for config and URL strategy) |







**Returns**


`Stream[Page]` - Stream emitting all pages (original + generated taxonomy pages)
:::{rubric} Examples
:class: rubric-examples
:::


```python
>>> pages = Stream.from_iterable(site.pages)
    >>> taxonomy_stream = create_taxonomy_stream(pages, site)
    >>> for page in taxonomy_stream.iterate():
    ...     print(page.output_path)
```



---
*Generated by Bengal autodoc from `bengal/bengal/pipeline/taxonomy.py`*
