
---
title: "menu"
type: "python-module"
source_file: "bengal/bengal/pipeline/menu.py"
line_number: 1
description: "Menu stream for building navigation menus from pages and sections. This module provides stream-based menu building that replaces MenuOrchestrator with a declarative, reactive approach. Flow: pages + s..."
---

# menu
**Type:** Module
**Source:** [View source](bengal/bengal/pipeline/menu.py#L1)



**Navigation:**
[bengal](/api/bengal/) ›[pipeline](/api/bengal/pipeline/) ›menu

Menu stream for building navigation menus from pages and sections.

This module provides stream-based menu building that replaces MenuOrchestrator
with a declarative, reactive approach.

Flow:
    pages + sections → build_menu_structure → site.menu populated

## Functions



### `create_menu_stream`


```python
def create_menu_stream(pages_stream: Stream[Page], site: Site) -> Stream[Page]
```



Create a stream that builds navigation menus from pages and sections.

This stream:
1. Collects all pages
2. Builds menu structure from config and page frontmatter
3. Populates site.menu with built menus
4. Returns pages unchanged (menus are side-effect on site)


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `pages_stream` | `Stream[Page]` | - | Stream of Page objects |
| `site` | `Site` | - | Site instance (menus are stored here) |







**Returns**


`Stream[Page]` - Stream emitting same pages (menus built as side-effect)
:::{rubric} Examples
:class: rubric-examples
:::


```python
>>> pages = Stream.from_iterable(site.pages)
    >>> menu_stream = create_menu_stream(pages, site)
    >>> # After iterating, site.menu is populated
    >>> list(menu_stream.iterate())
```



---
*Generated by Bengal autodoc from `bengal/bengal/pipeline/menu.py`*
