
---
title: "fix-css-nesting"
type: "python-module"
source_file: "scripts/fix-css-nesting.py"
line_number: 1
description: "Convert CSS nesting syntax to traditional selectors. This script finds all CSS nesting (&:hover, &.class, etc.) and converts them to traditional selectors that work in all browsers without transformat..."
---

# fix-css-nesting
**Type:** Module
**Source:** [View source](scripts/fix-css-nesting.py#L1)



**Navigation:**
[scripts](/api/scripts/) ›fix-css-nesting

Convert CSS nesting syntax to traditional selectors.

This script finds all CSS nesting (&:hover, &.class, etc.) and converts
them to traditional selectors that work in all browsers without transformation.

Usage: python scripts/fix-css-nesting.py [file1.css] [file2.css] ...
If no files provided, processes all CSS files in bengal/themes/default/assets/css/

## Functions



### `transform_css_nesting`


```python
def transform_css_nesting(content: str) -> str
```



Transform CSS nesting to traditional selectors.

Converts:
    .parent {
      color: red;
      &:hover { color: blue; }
      &.active { font-weight: bold; }
    }

To:
    .parent {
      color: red;
    }
    .parent:hover { color: blue; }
    .parent.active { font-weight: bold; }


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `content` | `str` | - | *No description provided.* |







**Returns**


`str`




### `process_file`


```python
def process_file(file_path: Path) -> bool
```



Process a single CSS file. Returns True if changes were made.


**Parameters:**

| Name | Type | Default | Description |
|:-----|:-----|:--------|:------------|
| `file_path` | `Path` | - | *No description provided.* |







**Returns**


`bool`




### `main`


```python
def main()
```



*No description provided.*



---
*Generated by Bengal autodoc from `scripts/fix-css-nesting.py`*

